{
    "layer": "top",
    "height": 26,
    "spacing": 5,
    // Choose the order of the modules
    "modules-left": [
        "hyprland/workspaces"
    ],
    "modules-center": [
        "custom/music"
    ],
    "modules-right": [
				"tray",
        "custom/pacman",
        "pulseaudio",
        "network",
        "battery",
				"clock",
        "custom/power"
		],
    "custom/music": {
        "format": "   {}",
        "escape": true,
        "interval": 5,
        "tooltip": false,
        "exec": "playerctl metadata --format='{{ title }}'",
        "on-click": "playerctl play-pause",
        "max-length": 50
    },
		"hyprland/window": {
			"separate-outputs": true
		},
    "hyprland/workspaces": {
        "disable-scroll": true,
        "all-outputs": true,
        "on-click": "activate"
    },
    "tray": {
        // "icon-size": 21,
        "spacing": 10
    },
		"custom/pacman": {
			"interval": 3600,
			"return-type": "json",
			"exec": "pacman-updates",
			"exec-if": "exit 0",
			"on-click": "$TERMINAL -e paru; pkill -SIGRTMIN+8 waybar",
			"signal": 8
		},
    "clock": {
        "tooltip-format": "{:%A, %B %d, %Y}",
        "format": "{:%H:%M}"
    },
    "battery": {
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{icon}",
        "format-icons": [ "󰂎", "󰁺", "󰁻", "󰁼", "󰁽", "󰁾", "󰁿", "󰂀", "󰂁", "󰂂", "󰁹" ],
        "format-charging": "󰂄",
        "format-warning": "󰂃",
				"tooltip-format": "{capacity}% capacity"
    },
    "network": {
        "format-wifi": "󰤨",
        "format-disconnected": "󰤭",
				"tooltip-format": "Connected to {essid}",
				"on-click": "hyprctl dispatch exec '[float] alacritty -e sh -c iwctl'"
    },
    "pulseaudio": {
        // "scroll-step": 1, // %, can be a float
        "format": "{icon}",
        "format-muted": "󰖁",
				"format-icons": {
					"default": ["", "", "󰕾"]
				},
				"tooltip-format": "{volume}% volume",
        "on-click": "pavucontrol"
		},
    "custom/power": {
        "format": "󰐥",
        "on-click": "sleep 0.15 && wlogout"
    }
}

// vim: ft=jsonc

