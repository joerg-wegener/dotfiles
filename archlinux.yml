- name: ArchDevelop1
  hosts: localhost
  tasks:
    - name: "System update"
      community.general.pacman:
        update_cache: true
        upgrade: true
    - name: "Ensure stow installed"
      become: yes
      pacman:
        name: stow
        state: present
